import { css } from "@emotion/react";

export const pageLayoutHeaderTitle = css`
  font-size: 1.60188rem;
  font-style: normal;
  font-weight: 700;
  line-height: normal;
`;

export const pageLayoutHeaderBtn = css`
  border-radius: 0.625rem;
  background-color: var(--light-version-primary, #3a36db);
`;
