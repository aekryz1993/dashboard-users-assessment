export enum INVOICE_STATUS {
  PENDING = "PENDING",
  COMPLETE = "COMPLETE",
  CANCEL = "CANCEL",
}

export enum SORT_DIRECTION {
  asc = "asc",
  desc = "desc",
}

export enum INVOICES_SORT_COLUMN {
  ID = "ID",
  NAME = "NAME",
  EMAIL = "EMAIL",
  DATE = "DATE",
  STATUS = "STATUS",
}