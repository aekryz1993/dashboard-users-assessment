export enum INVOICE_STATUS {
  PENDING = "PENDING",
  COMPLETE = "COMPLETE",
  CANCEL = "CANCEL",
}

export enum SORT_DIRECTION {
  ASC = "ASC",
  DESC = "DESC",
}

export enum INVOICES_SORT_COLUMN {
  NAME = "NAME",
  EMAIL = "EMAIL",
  DATE = "DATE",
  STATUS = "STATUS",
}
